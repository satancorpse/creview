const state = {
    show: false,
    data: ''
};

const actions = {

}

const mutations = {

};

const getters = {
    show: state => state.show,
    data: state => state.data
};

export default {
    state,
    getters,
    actions,
    mutations
};
